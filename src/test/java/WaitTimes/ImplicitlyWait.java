package WaitTimes;

public class ImplicitlyWait {
    /*
    driver.manage.waitTimes().ImplicitlyWait(Duration.ofSeconds(10)
    --> It waits the elements for a specific time(10,20,30...etc)
    --> It is different than Thread.sleep because Thread.sleep(3000) you have to wait for exactly the time that you provide
    --> In Implicity Wait --> It will wait from 1 sec to 10 sec(max). If element gets load in 2 sec --> it will continue
    --> We use imlicity wait under Driver. Helper class to make sure
     */
}
